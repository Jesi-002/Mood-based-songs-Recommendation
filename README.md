# Mood-based-songs-Recommendation
The Mood-Based Music Recommendation System is a C programming project that recommends songs based on the user's mood. It allows users to add songs, search for songs, rate them and manage favourite songs. The project uses basic C concepts such as arrays, functions, strings, loops, searching, input from user and conditional statements.
